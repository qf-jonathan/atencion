import { FilterDetailPipe } from './filter-detail.pipe';

describe('FilterPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterDetailPipe();
    expect(pipe).toBeTruthy();
  });
});
